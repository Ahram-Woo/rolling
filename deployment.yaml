apiVersion: apps/v1
kind: Deployment
metadata:
  name: rolling-update-deploy
spec:
  selector:
    matchLabels:
      app: mynginx
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: mynginx
    spec:
      containers:
      - name: nginx
        image: mynginx
        ports:
        - containerPort: 80 
